export enum CtxKeys {
  BUS = 'ctx:bus',
  REGISTRY = 'ctx:registry',
  IS_PROCESSED = 'ctx:processed',
  CURRENT_PHASE = 'ctx:current_phase',
  ERROR_PHASE = 'ctx:error_phase',
  LAST_ERROR = 'ctx:last_error',
}
